spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:care_service}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres123}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=true

token.signing.key=${JWT_SECRET:53A73E5F1C4E0A2D3B5F2D784E6A1B423FFF247D1F6E5C3A596D635A75327855}

# ??????
server.port=${SERVER_PORT:8080}
server.forward-headers-strategy=framework

# Docker
spring.docker.compose.enabled=false

# Spring Security (??? ????????????)
spring.security.user.name=user
spring.security.user.password=password

# Swagger
springdoc.api-docs.enabled=true
springdoc.swagger-ui.version=3.0.1